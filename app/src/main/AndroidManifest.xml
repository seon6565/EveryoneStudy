<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comwow2778.naver.blog.everyonestudy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Roading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".finance" />
        <activity android:name=".engineer" />
        <activity android:name=".language" />
        <activity android:name=".basic" />
        <activity android:name=".community" />
        <activity android:name=".internet" />
        <activity android:name=".Computer" />
        <activity android:name=".html" />
        <activity android:name=".htmldata" />
        <activity android:name=".userspace"></activity>
    </application>

</manifest>